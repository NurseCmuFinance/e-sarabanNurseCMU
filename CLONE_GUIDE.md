# 📖 คู่มือการโคลนระบบ E-Saraban และการตั้งค่าสำหรับหน่วยงานใหม่

ยินดีต้อนรับสู่ระบบ **E-Saraban (สารบรรณอิเล็กทรอนิกส์)** คู่มือฉบับนี้จัดทำขึ้นเพื่อให้หน่วยงาน, คณะ หรือแผนกอื่น ๆ ที่ต้องการนำระบบนี้ไปใช้งานเป็นของตนเอง สามารถโคลนโค้ดและตั้งค่าระบบ (Database & Storage) แยกเป็นเอกเทศได้อย่างสมบูรณ์

---

## 🛠️ ขั้นตอนที่ 1: การโคลนโค้ดโปรเจกต์ (Cloning Project)

ให้ทำการดาวน์โหลดหรือโคลนซอร์สโค้ดนี้จาก GitHub ไปที่คอมพิวเตอร์ของคุณ:

```bash
git clone https://github.com/NurseCmuFinance/e-sarabanNurseCMU.git
```

---

## 💾 ขั้นตอนที่ 2: การสร้างและติดตั้งฐานข้อมูล Supabase

ระบบสารบรรณนี้ใช้ **Supabase** เป็นฐานข้อมูลหลัก (Database) และระบบจัดการสิทธิ์ผู้ใช้งาน (Auth)

1. เข้าไปที่ [Supabase.com](https://supabase.com) จากนั้นสมัครสมาชิกและสร้างโปรเจกต์ใหม่ (**New Project**)
2. เมื่อโปรเจกต์สร้างเสร็จแล้ว ให้ไปที่เมนู **SQL Editor** (เมนูรูปไอคอนคิวรี่ทางซ้ายมือ)
3. กดปุ่ม **New Query** จากนั้นเปิดดูเนื้อหาในไฟล์ `supabase_schema.sql` (ที่อยู่ในโฟลเดอร์หลักของโปรเจกต์นี้) 
4. คัดลอกคำสั่ง SQL ทั้งหมดในไฟล์ดังกล่าว นำไปวางในช่อง SQL Editor ของ Supabase แล้วกดปุ่ม **Run**
   > [!NOTE]
   > คำสั่งนี้จะทำการสร้างตารางข้อมูลทั้งหมดที่จำเป็นสำหรับระบบ เช่น ตารางเก็บประวัติเอกสาร (`documents`), รายการบันทึกสถานะ (`document_logs`), ข้อความแชท (`messages`), โปรไฟล์ผู้ใช้ (`profiles`) รวมถึงการตั้งค่า Triggers อัตโนมัติให้เสร็จสมบูรณ์ในคลิกเดียว

---

## 🔗 ขั้นตอนที่ 3: แก้ไขคีย์เชื่อมต่อฐานข้อมูลในโค้ด

1. ในโฟลเดอร์โปรเจกต์ที่คุณโคลนมา ให้เปิดไฟล์ `supabaseClient.ts`
2. ทำการแก้ไข **Project URL** และ **API Anon Key** ให้เป็นข้อมูลโปรเจกต์ Supabase ใหม่ของคุณ (ดูได้จากเมนู *Project Settings > API* ในหน้าแดชบอร์ด Supabase)
3. เปลี่ยนค่าในไฟล์ดังนี้:
   ```typescript
   const SUPABASE_URL = 'https://<รหัสโปรเจกต์ใหม่ของคุณ>.supabase.co';
   const SUPABASE_ANON_KEY = '<รหัส Anon Key ของโปรเจกต์ใหม่ของคุณ>';
   ```
4. กดบันทึกไฟล์

---

## 🚀 ขั้นตอนที่ 4: ทดสอบการรันระบบและนำขึ้นระบบจริง (Deployment)

### ทดสอบการทำงานบนเครื่องของตนเอง (Local Test):
เปิด Command Line ในโฟลเดอร์โปรเจกต์แล้วรันคำสั่ง:
```bash
# ติดตั้งเครื่องมือและไลบรารีที่จำเป็น
npm install

# รันระบบจำลองในเครื่องคอมพิวเตอร์ของคุณ
npm run dev
```

### การนำขึ้นเซิร์ฟเวอร์เพื่อให้คนอื่นใช้งานได้จริง (Production):
คุณสามารถนำโปรเจกต์ขึ้นระบบคลาวด์โฮสติ้ง เช่น **Vercel** (แนะนำที่สุดและใช้งานฟรี) 
1. สมัครสมาชิกเว็บ [Vercel.com](https://vercel.com)
2. กดปุ่ม **Add New > Project** แล้วเลือกโปรเจกต์ GitHub ที่คุณโคลนไว้
3. กดปุ่ม **Deploy** ระบบ Vercel จะแปลงหน้าเว็บและสร้าง URL เว็บไซต์หลักสำหรับหน่วยงานของคุณให้พร้อมใช้งานทันทีภายใน 1 นาที!

---

## 🔑 ขั้นตอนที่ 5: การสร้างผู้ดูแลระบบ (First Admin Setup)

เมื่อนำระบบขึ้นโฮสต์จริงแล้ว คุณต้องสร้างบัญชีแอดมินคนแรกเพื่อเข้าไปบริหารจัดการระบบ:

1. เข้าไปที่หน้าเว็บระบบสารบรรณใหม่ของคุณ แล้วทำการ **"สมัครสมาชิกใหม่" (Register)** ตามปกติ
2. เข้าไปที่หน้าแดชบอร์ดของ **Supabase** เลือกเมนู **Table Editor** แล้วไปที่ตารางที่ชื่อว่า `profiles`
3. ค้นหาแถวที่เป็นบัญชีผู้ใช้ที่คุณเพิ่งสมัครเข้ามา จากนั้นให้ดับเบิลคลิกแก้ไขช่องคอลัมน์ `role` จากเดิมที่เป็น `'user'` ให้แก้คำว่า **`'admin'`** จากนั้นกดบันทึก
4. เมื่อแอดมินคนดังกล่าวนำบัญชีไปล็อกอินใหม่ที่หน้าเว็บ จะพบแท็บเครื่องมือพิเศษ **"ตั้งค่าระบบ (Admin Settings)"** ปรากฏขึ้นมาที่แถบเมนูด้านข้างทันที!

---

## 📁 ขั้นตอนที่ 6: การเชื่อมต่อ Google Drive API ของหน่วยงานตนเอง

เพื่อให้ไฟล์แนบทั้งหมดของหน่วยงานใหม่ ถูกอัปโหลดไปจัดเก็บไว้ใน Google Drive ของหน่วยงานตนเองอย่างปลอดภัย ให้ทำตามขั้นตอนการขอสิทธิ์ดังนี้ครับ:

### A) ขอคีย์เชื่อมต่อจาก Google Cloud Console:
1. เข้าไปที่หน้าเว็บ [Google Cloud Console](https://console.cloud.google.com) ด้วยบัญชีอีเมล Google ขององค์กรหรือหน่วยงานของคุณ
2. กดเลือกหรือสร้างโปรเจกต์ใหม่ (**New Project**) เช่นตั้งชื่อว่า `e-saraban-storage`
3. ในช่องค้นหาด้านบน พิมพ์ค้นหาคำว่า **"Google Drive API"** คลิกเข้าไปแล้วกดปุ่ม **Enable (เปิดใช้งาน)**
4. ไปที่เมนู **APIs & Services** > **OAuth consent screen** ทางซ้ายมือ:
   * เลือกประเภท User Type เป็น **Internal** (สำหรับให้สมาชิกระดับองค์กรเชื่อมต่อเพื่อใช้งานในแผนกได้อย่างปลอดภัยที่สุด) หรือ **External** (หากต้องการรองรับสมาชิกภายนอก)
   * กรอกชื่อแอปพลิเคชัน (เช่น `E-Saraban App`) และอีเมลสำหรับติดต่อผู้ดูแลระบบ จากนั้นกด **Save and Continue** จนเสร็จสิ้นขั้นตอน
5. ไปที่เมนู **APIs & Services** > **Credentials** ทางซ้ายมือ:
   * กดปุ่ม **+ CREATE CREDENTIALS** ด้านบน เลือกหัวข้อ **OAuth client ID**
   * **Application type:** ให้เลือกเป็น **Web application**
   * **Authorized redirect URIs:** ให้กดปุ่ม **+ ADD URI** แล้วเพิ่ม URL หน้าเว็บระบบสารบรรณของคุณ ตัวอย่างเช่น:
     * `https://e-saraban-new-department.vercel.app` (URL เว็บจริงบน Vercel)
     * `http://localhost:5173` (สำหรับกรณีเปิดทดสอบในเครื่องส่วนตัว)
   * กดปุ่ม **Create** ระบบจะสร้างคีย์และเปิดกล่องข้อความที่มีรหัส **Client ID** และ **Client Secret** ขึ้นมา ให้คัดลอกรหัสสองตัวนี้เก็บไว้

### B) สร้างโฟลเดอร์ปลายทางเก็บไฟล์:
1. เปิดหน้า Google Drive ของหน่วยงานคุณ แล้วสร้างโฟลเดอร์สำหรับจัดเก็บเอกสารสารบรรณ เช่น `คลังเอกสารแนบ E-Saraban`
2. ดับเบิลคลิกเปิดเข้าไปในโฟลเดอร์ที่เพิ่งสร้าง แล้วสังเกตแถบลิงก์ที่แสดงอยู่บนหัวเว็บบราวเซอร์ของคุณ:
   * รูปแบบลิงก์จะเป็น: `https://drive.google.com/drive/folders/1CCidD8V1lEVy4RwcAG4CBkOeTDTR7aij`
   * รหัสภาษาอังกฤษผสมตัวเลขยาว ๆ ด้านหลังสุดของลิงก์ (เช่น `1CCidD8V1lEVy4RwcAG4CBkOeTDTR7aij`) คือ **Target Folder ID** ให้คุณคัดลอกรหัสนี้เก็บไว้

### C) เชื่อมโยงบัญชีและใช้งาน:
1. นำข้อมูล **Client ID**, **Client Secret** และ **Target Folder ID** ที่บันทึกเก็บไว้ทั้งหมด ไปกรอกในช่องกรอกข้อมูลในหน้า **"ตั้งค่าระบบ (Admin Settings)"** บนเว็บไซต์สารบรรณใหม่ของคุณ
2. กดปุ่ม **"บันทึกค่าและเชื่อมต่อ Google Drive"**
3. ระบบจะนำคุณเข้าสู่หน้าต่างขอสิทธิ์การเข้าถึงไฟล์ของ Google ให้กดเลือกบัญชีอีเมลหลักของหน่วยงานและกด **"อนุญาต (Allow)"** 
4. **เรียบร้อย!** หน้าต่างระบบจะแจ้งเตือนความสำเร็จ และระบบสารบรรณของหน่วยงานใหม่ของคุณจะเข้าสู่สถานะจัดเก็บเอกสารแนบทุกฉบับเข้าสู่ Google Drive ส่วนตัวของคุณทันทีอย่างเป็นระเบียบและสมบูรณ์แบบครับ! 🎉

---
